DELIMITER //

CREATE FUNCTION GetSupplierCount() RETURNS INT
BEGIN
    DECLARE totalSuppliers INT;
    SELECT COUNT(*) INTO totalSuppliers FROM Supplier;
    RETURN totalSuppliers;
END //

DELIMITER ;
